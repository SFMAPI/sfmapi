# Read the Docs build config — see https://docs.readthedocs.io/page/config-file/v2.html

version: 2

build:
  os: ubuntu-24.04
  tools:
    python: "3.12"
  jobs:
    post_install:
      # Install runtime + sdk so autodoc can introspect them.
      - pip install -e .
      - pip install -e clients/python
      - pip install -r docs/requirements.txt

sphinx:
  configuration: docs/conf.py
  # Strict build matches the .github/workflows/docs.yml gate.
  # Local repro: `python -m sphinx -b html -W --keep-going docs docs/_build/html`.
  fail_on_warning: true

formats:
  - htmlzip

python:
  install: []   # handled in post_install above
